# CrossHop App (Frontend)
React + Vite starter.

## Scripts
- `npm run dev` - start dev server (uses `--host` so Replit works)
- `npm run build` - production build
- `npm run preview` - preview build

Ensure `vite.config.js` has `server.allowedHosts = ['*']` when using Replit.